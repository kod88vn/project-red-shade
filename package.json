{
  "scripts": {
    "client": "parcel build client/src/index.html --out-dir build/dist --cache-dir build/cache",
    "server": "node server/src/index.js",
    "dev": "CLIENT_ENABLED=true node server/src/index.js",
    "docker-build": "docker build -t redshade ./",
    "docker-run": "docker run --rm -p 5678:5678/tcp redshade:latest",
    "compose": "docker-compose up"
  }
}
